---
description: Подхватить незавершённую работу AgentOS
---

```bash
agentctl resume
```

Выполни выданные задания и верни результаты через `agentctl task report`.
Если заданий нет, а миссия висит — посмотри, чего она ждёт:
`agentctl status`.
